For this lab, I extended the starter feature selection notebook by adding a Logistic Regression experiment on the selected feature subset. I applied train-test splitting with stratification, standardized the features, and evaluated the model using multiple metrics including accuracy, ROC-AUC, precision, recall, and F1-score. This modification provided a comparative evaluation beyond the original workflow and demonstrated a more complete model assessment.
